## syntax: indent
## validate ver: \S*
## validate default: [a-z0-9_-]+
# generated by verwalter {{ verwalter_version | ver }}

image-dir: /var/lib/code-images
image-dir-levels: 3
readonly-paths:
  /secrets: /var/lib/secrets/{{ role }}

allow-users: [0-1]
allow-groups: [0-1]

uid-map:
# TODO(tailhook) allocations
- { inside: 0, outside: 10000, count: 2 }
gid-map:
# TODO(tailhook) allocations
- { inside: 0, outside: 10000, count: 2 }
