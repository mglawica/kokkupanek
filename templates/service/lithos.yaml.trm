## syntax: indent
## validate ver: \S*
## validate default: [a-zA-Z0-9_./-]+
## validate filename: [^\n\r"]+
# Generated by verwalter {{ verwalter_version }}

## for name, daemon in services
    {{ name }}:
        kind: Daemon
        image: {{ daemon.image }}
        config: "{{ daemon.config | filename }}"
        instances: {{ daemon.instances }}
## endfor
